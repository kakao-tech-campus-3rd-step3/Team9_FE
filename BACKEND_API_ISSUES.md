# 백엔드 API 확인 사항 (신청자 승인/거절 기능)

## 문제 상황

프론트엔드에서 신청자 승인/거절 기능이 정상적으로 작동하지 않습니다.

## 현재 프론트엔드 구현 상태

### 1. API 호출 정보

- **엔드포인트**: `PATCH /api/studies/{study_id}/applications/{application_id}`
- **요청 본문**:
  ```json
  {
    "status": "Accepted" // 또는 "Rejected"
  }
  ```

### 2. 사용 중인 상태 값

- `"Accepted"` - 승인
- `"Rejected"` - 거절
- `"Pending"` - 대기 (참고용)

## 백엔드 확인 필요 사항

### ❗ 필수 확인 항목

#### 1. 상태 값(Status) Enum 확인

**질문**: `status` 필드가 받아야 하는 정확한 값은 무엇인가요?

현재 프론트엔드에서 보내는 값:

- `"Accepted"` (승인)
- `"Rejected"` (거절)

가능한 다른 형식들:

- `"APPROVED"` / `"REJECTED"` (대문자)
- `"approved"` / `"rejected"` (소문자)
- `"accept"` / `"reject"` (약식)
- 다른 enum 값?

**요청**: 백엔드에서 정확히 어떤 값을 기대하는지 확인 필요합니다.

#### 2. 에러 응답 형식 확인

현재 프론트엔드가 처리하는 에러 코드:

- `409 Conflict` - 인원 초과 등
- `403 Forbidden` - 권한 없음
- `400 Bad Request` - 잘못된 요청

**질문**:

1. 인원 초과 시 어떤 에러 코드와 메시지를 반환하나요?
2. 이미 처리된 신청을 다시 처리하려 할 때 어떤 에러를 반환하나요?
3. `INVALID_STATE_CHANGE` 같은 커스텀 에러 코드가 있나요?

**요청**: 각 에러 케이스별 응답 형식 문서화 필요

**예상 에러 응답 형식**:

```json
{
  "code": "ERROR_CODE",
  "message": "사용자에게 표시할 메시지",
  "errors": [],
  "details": "상세 정보 (선택사항)"
}
```

#### 3. 경로 파라미터 확인

- API 문서: `/api/studies/{study_id}/applications/{app_id}`
- 프론트엔드 사용: `/api/studies/{study_id}/applications/{application_id}`

**질문**: `app_id`와 `application_id` 모두 동일하게 동작하나요?

#### 4. 성공 응답 형식 확인

**질문**: 성공 응답의 정확한 형식은 무엇인가요?

현재 프론트엔드가 기대하는 형식:

```json
{
  "success": true,
  "message": "성공 메시지"
}
```

또는 다른 형식인가요? (예: 204 No Content, 다른 필드명 등)

### 🔍 추가 확인 사항

#### 5. 동시성 처리

여러 관리자가 동시에 같은 신청을 처리할 때 어떻게 동작하나요?

- 마지막 요청이 우선인가요?
- 첫 번째 요청만 성공하고 나머지는 실패인가요?

#### 6. 인원 수 검증 로직

**질문**:

1. 현재 멤버 수가 최대 인원과 같을 때 승인 요청하면 어떻게 되나요?
2. 최대 인원을 초과하는 승인 요청이 들어오면 어떤 에러를 반환하나요?
3. 인원 수 검증은 백엔드에서 자동으로 하나요?

#### 7. 신청 상태 전환 가능 여부

**질문**:

- `Pending` → `Accepted` (가능?)
- `Pending` → `Rejected` (가능?)
- `Accepted` → `Rejected` (가능?)
- `Rejected` → `Accepted` (가능?)

각 상태 전환이 가능한지, 불가능하다면 어떤 에러를 반환하는지 확인 필요

## 프론트엔드 현재 동작

### 콘솔 로그 확인 포인트

개발자 도구 콘솔에서 다음 로그를 확인할 수 있습니다:

1. `[API 호출] 신청 상태 변경` - 요청 정보
2. `[API 응답] 신청 상태 변경 성공` - 성공 응답
3. `[API 에러] 신청 상태 변경 실패` - 에러 발생 시
4. `[API 에러 상세]` - 에러 상세 정보

### 디버깅 정보

프론트엔드가 자동으로 로깅하는 정보:

- 요청 URL
- 요청 본문 (status 값)
- 응답 상태 코드
- 응답 데이터
- 에러 메시지

## 요청 사항 요약

1. ✅ **상태 값 Enum 명확화**: `status` 필드가 받는 정확한 값 확인
2. ✅ **에러 응답 표준화**: 각 에러 케이스별 응답 형식 문서화
3. ✅ **성공 응답 형식**: 성공 시 응답 구조 확인
4. ✅ **비즈니스 로직 확인**: 인원 수 검증, 상태 전환 규칙 확인

## 실제 테스트 시 확인 사항

### 브라우저 콘솔에서 확인할 정보

신청 승인/거절 버튼 클릭 시 콘솔에 다음 정보가 출력됩니다:

1. **요청 전송 정보**:

   ```
   [API 호출] 신청 상태 변경
   {
     url: "/api/studies/{study_id}/applications/{application_id}",
     method: "PATCH",
     requestBody: { status: "Accepted" } // 또는 "Rejected"
   }
   ```

2. **에러 발생 시**:
   ```
   [API 에러] 신청 상태 변경 실패: ...
   [API 에러 상세] {
     status: 409, // 또는 다른 상태 코드
     data: { ... }, // 에러 응답 본문
     url: "...",
     requestData: "..."
   }
   ```

### 백엔드에 전달할 실제 에러 정보

위 콘솔 로그를 스크린샷으로 캡처하거나, 다음 정보를 백엔드 팀에 공유:

1. **요청 URL**:
   `/api/studies/{실제_study_id}/applications/{실제_application_id}`
2. **요청 본문**: `{ "status": "Accepted" }` (또는 "Rejected")
3. **응답 상태 코드**: (예: 400, 403, 409 등)
4. **응답 본문**: 전체 에러 메시지

## 예상되는 문제 시나리오

### 시나리오 1: 인원 초과

- **상황**: 현재 멤버 수가 최대 인원과 같거나 초과
- **예상 에러**: 409 Conflict
- **백엔드 확인**: 인원 검증 로직이 정확히 작동하는지

### 시나리오 2: 상태 값 불일치

- **상황**: 백엔드가 기대하는 status 값과 프론트엔드가 보내는 값이 다름
- **예상 에러**: 400 Bad Request
- **백엔드 확인**: status enum 정의

### 시나리오 3: 권한 문제

- **상황**: 스터디 리더가 아닌 사용자가 승인 시도
- **예상 에러**: 403 Forbidden
- **백엔드 확인**: 권한 검증 로직

### 시나리오 4: 이미 처리된 신청

- **상황**: 이미 Accepted/Rejected된 신청을 다시 처리 시도
- **예상 에러**: 409 Conflict 또는 400 Bad Request
- **백엔드 확인**: 상태 전환 가능 여부 검증

## 참고: API 문서 스크린샷 정보

- 엔드포인트: `PATCH /api/studies/{study_id}/applications/{app_id}`
- 요청 본문 예시: `{ "status": "Pending" }` (예시일 가능성)
- 실제 사용해야 할 값: `"Accepted"` 또는 `"Rejected"` (확인 필요)

---

**작성일**: 2025-01-XX  
**담당**: 프론트엔드 팀  
**우선순위**: 🔴 높음 (신청 승인/거절 기능이 핵심 기능)
